[build]
  command = "npm --prefix client ci && npm --prefix server ci && npm --prefix client run build"
  publish = "client/dist"
  functions = "server/netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
